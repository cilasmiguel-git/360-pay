// Generated by dts-bundle-generator v9.5.1

/**
 * Options to use when creating the REST client.
 */
export interface RESTOptions {
	/**
	 * Base URL without the version for all requests (Default `https://api.abacatepay.com/`).
	 */
	base?: string;
	/**
	 * The API key to use in the Authorization header.
	 */
	secret?: string;
	/**
	 * The time to wait in milliseconds before a request is aborted (Default `5_000`).
	 */
	timeout?: number;
	/**
	 * Options to use when retrying.
	 */
	retry?: RetryOptions;
	/**
	 * Version to use in requests (Default 1).
	 */
	version?: string | number;
	/**
	 * Additional headers to send for all API requests.
	 */
	headers?: Record<string, string>;
	/**
	 * Function to execute when a rate limit occurs.
	 * @param response - Response of the rate limit.
	 */
	onRateLimit?(response: Response): unknown;
}
/**
 * Options to use when sending a request to the API.
 */
export interface MakeRequestOptions extends Pick<RESTOptions, "retry" | "headers"> {
	/**
	 * Optional data to send in the request body (We stringify with JSON.stringify internally).
	 */
	body?: unknown;
	/**
	 * Method to use in the request.
	 */
	method: HTTPMethodLike;
	/**
	 * Query string parameters to append to the called endpoint.
	 */
	query?: string | Record<string, string> | URLSearchParams | [
		string,
		string
	][];
}
/**
 * Options to use when sending a request to the API (Without the method).
 */
export type MakeRequestOptionsWithoutMethod = Omit<MakeRequestOptions, "method">;
/**
 * Options to use when retrying a request.
 */
export interface RetryOptions {
	/**
	 * Maximum number of attempts we should try.
	 */
	max: number;
	/**
	 * Custom function to set the backoff of the retry.
	 * @param attempt Current retry attempt.
	 */
	backoff?(attempt: number): number;
	/**
	 * Function to execute every time a retry is made
	 * @param context The context for the retry
	 */
	onRetry?(context: RetryContext): unknown;
}
/**
 * Context provided when a retry is made.
 */
export interface RetryContext {
	/**
	 * Current attempt number.
	 */
	attempt: number;
	/**
	 * Response received from the API, undefined if the error was a timeout.
	 */
	response?: Response;
	/**
	 * Options used in the request.
	 */
	options: MakeRequestOptions;
}
/**
 * Any HTTP method.
 */
export type HTTPMethodLike = "GET" | "PUT" | "POST" | "HEAD" | "PATCH" | "DELETE" | (string & {});
/**
 * @private
 */
export interface InternalHandleErrorOptions {
	route: string;
	attempt: number;
	response: Response;
	retry: RetryOptions;
	requestOptions: MakeRequestOptions;
}
/**
 * @private
 */
export type InternalHandleTimeoutErrorOptions = Omit<InternalHandleErrorOptions, "response">;
/**
 * Creates a REST client for the AbacatePay API.
 *
 * Every method resolves — it never rejects or throws. Client-side failures
 * (network errors, timeouts, missing secret) are normalized into the same
 * `{ data, error, success }` shape a real API response has.
 */
export declare const createREST: (options?: RESTOptions) => {
	/**
	 * Options used in all requests. Mutating this object (e.g. via
	 * `setSecret`) affects every subsequent call made with this client.
	 */
	options: RESTOptions;
	/**
	 * Sets the authorization token that should be used for requests.
	 * @param secret The secret to use.
	 */
	setSecret(secret: string): any;
	/**
	 * Runs a GET request from the API.
	 */
	get<R>(route: string, requestOptions?: MakeRequestOptionsWithoutMethod): Promise<R>;
	/**
	 * Runs a POST request from the API.
	 */
	post<R>(route: string, requestOptions?: MakeRequestOptionsWithoutMethod): Promise<R>;
	/**
	 * Runs a DELETE request from the API.
	 */
	delete<R>(route: string, requestOptions?: MakeRequestOptionsWithoutMethod): Promise<R>;
	/**
	 * Runs a PUT request from the API.
	 */
	put<R>(route: string, requestOptions?: MakeRequestOptionsWithoutMethod): Promise<R>;
	/**
	 * Runs a PATCH request from the API.
	 */
	patch<R>(route: string, requestOptions?: MakeRequestOptionsWithoutMethod): Promise<R>;
};
/**
 * A REST client for the AbacatePay API, as returned by {@link createREST}.
 */
export type REST = ReturnType<typeof createREST>;
/**
 * Array of any retryable status code.
 */
export declare const RETRYABLE_STATUS: number[];
/**
 * Current version of [@abacatepay/rest](https://www.npmjs.com/package/@abacatepay/rest).
 */
export declare const version: "1.0.2";
/**
 * Current version of the AbacatePay API.
 */
export declare const API_VERSION: "2";

export {};
